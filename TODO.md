* `Weekday` is a confusing name for an enum that also contains weekends. Maybe `DayOfWeek` would be better.
* The docsite is looking a bit sparse, and we don't have any tests. Granted, we don't really have much *behavior* yet, but adding descriptions and doctests showing usage would be good.
